## 📦 Installation

1. Create the conda environment:

```bash
conda env create -f environment.yml
```

2. Activate the environment:

```bash
conda activate gasmr
```

---

## 🚀 Running

Run the main script:

```bash
python main.py
```

Modify the configuration in `main.py` to test GAS under different settings.
